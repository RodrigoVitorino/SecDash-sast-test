rootProject.name = "sast-test"
```

---

**Estrutura do repo deve ficar assim:**
```
sast-test/
├── build.gradle.kts
├── settings.gradle.kts
└── src/
    └── main/
        └── kotlin/
            └── Main.kt      ← o ficheiro com o código vulnerável
